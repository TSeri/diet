# ダイエット記録アプリ 仕様書

最終更新：2026-09-20 / 対象バージョン：**1.14.1**

---

## 1. 概要

体重・体脂肪率を軸に、食事・アルコール・睡眠・排泄・バイタル・室内環境までを1つの端末内に記録する、**HTML1枚のオフラインアプリ**。

| 項目 | 内容 |
|---|---|
| 形態 | 静的ファイルのみ。サーバー処理なし |
| 依存 | 外部ライブラリ・CDN・フォント・API すべてなし |
| 通信 | 起動後は一切の外部通信を行わない |
| 保存先 | 実行端末の `localStorage` のみ |
| 想定端末 | iPhone（Safari／ホーム画面追加）とPCブラウザ |
| 言語 | 日本語のみ |

### 設計方針

1. **オフラインで完結する。** ネットワークが無くても全機能が動くこと。
2. **データを外に出さない。** 記録は端末内のみ。書き出しはユーザーの明示操作でのみ行う。
3. **1タップで終わる記録を用意する。** 毎日続ける記録（起床・就寝・排泄）は入力欄を挟まない。
4. **未入力を許容する。** 測定値はどれか1つ埋まっていれば保存できる。
5. **あとから直せる。** すべての記録は日時を含めて編集・削除できる。

---

## 2. ファイル構成

| ファイル | 役割 | 必須 |
|---|---|---|
| `index.html` | アプリ本体（HTML・CSS・JSすべて内包） | ○ |
| `manifest.webmanifest` | ホーム画面追加時の名前・アイコン・表示モード | △ |
| `sw.js` | オフラインキャッシュ用 Service Worker | △ |
| `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` | アイコン | △ |
| `README.md` / `SPEC.md` | ドキュメント | − |

`index.html` 単体でも全機能が動作する。△ の4ファイルは「ホーム画面に追加してオフラインで使う」ためだけに必要。

### 配信方法

| 開き方 | オフライン動作 | 更新の反映 |
|---|---|---|
| `https://` で配信（GitHub Pages 等） | Service Worker が有効。機内モードでも起動 | 「更新を確認」または開き直し |
| `file://` でローカルのHTMLを直接開く | Service Worker は登録されない | 常に最新のファイルが読まれる |

配信URL配下のサブパス（例 `https://<user>.github.io/diet/`）で動くよう、参照はすべて相対パスで書く。

---

## 3. 画面構成

下部タブで4画面を切り替える。最後に開いていたタブは `localStorage` に保存し、次回起動時に復元する。

| タブ | 役割 |
|---|---|
| **記録** | 画面上部に固定された共通の日時と入力切り替えタブ（生活／食事／体組成／歩数）。選んだ入力カードを1枚だけ表示し、その下にサマリーを置く |
| **グラフ** | 折れ線1つ（3モード切替）と棒グラフ3つ |
| **履歴** | 全記録を日付ごとに新しい順で一覧。タップで編集、🗑 で削除 |
| **設定** | プロフィール・目標、データ書き出し／読み込み、バージョン、全削除 |

iPhone対応として、セーフエリア（ノッチ・ホームインジケータ）の余白確保、入力欄のフォントサイズ16px以上（フォーカス時の自動ズーム防止）、`apple-mobile-web-app-capable` によるスタンドアロン表示に対応する。配色はOSのライト／ダークモードに追従する。

---

## 4. データモデル

`localStorage` のキー **`diet-tracker-v1`** に、以下のJSONを1件だけ保存する。
（別キー `diet-tracker-v1:view` に最後に開いたタブ名を保存する）

```jsonc
{
  "body":  [ /* 測定記録 */ ],
  "meals": [ /* 食事記録 */ ],
  "logs":  [ /* 生活記録 */ ],
  "steps": { "2026-08-25": 8432 },   /* 日付 → 歩数 */
  "settings": { "height":null, "goal":null, "kcal":null, "protein":null, "alcLimit":null, "stepGoal":null },
  "v": 1
}
```

`id` は `Date.now()` の36進表記＋乱数6桁。`dt` はすべてISO8601（UTC）で保存し、表示時に端末のローカル時刻へ変換する。

### 4.1 body — 測定記録（体組成・バイタル・環境）

| キー | 内容 | 型 | 入力範囲 |
|---|---|---|---|
| `id` / `dt` | ID / 測定日時 | string | − |
| `weight` | 体重 (kg) | number\|null | 0 < w ≤ 500 |
| `fat` | 体脂肪率 (%) | number\|null | 0〜100 |
| `temp` | 体温 (℃) | number\|null | 30〜45 |
| `spo2` | SpO₂ (%) | number\|null | 50〜100 |
| `pulse` | 脈拍 (bpm) | number\|null | 20〜250 |
| `weather` | 天気 | string\|null | 快晴／晴れ／くもり／雨／雪／雷雨 |
| `room` | 室温 (℃) | number\|null | −20〜60 |
| `humid` | 湿度 (%) | number\|null | 0〜100 |
| `memo` | メモ | string | − |

**保存条件**：8項目（メモを除く）のうち **少なくとも1つ** に入力があること。範囲外の値は保存せず、どの項目が範囲外かを示して中断する。

### 4.2 meals — 食事記録

| キー | 内容 | 型 | 入力範囲 |
|---|---|---|---|
| `id` / `dt` | ID / 日時 | string | − |
| `type` | 区分 | string | 朝食／昼食／夕食／間食／飲み物 |
| `name` | 内容 | string | **必須** |
| `kcal` | カロリー | number\|null | 0〜20000 |
| `protein` | たんぱく質 (g) | number\|null | 0〜1000 |
| `salt` | 塩分・食塩相当量 (g) | number\|null | 0〜100 |
| `ml` | 酒量 (ml) | number\|null | 0〜5000 |
| `abv` | 度数 (%) | number\|null | 0〜100 |
| `alc` | 純アルコール (g) | number\|null | `ml`・`abv` から自動計算 |
| `memo` | メモ | string | − |

**保存条件**：`name` が必須。`ml` と `abv` は **両方入力するか、両方空** にする（片方だけでは純アルコール量を計算できないため中断する）。

### 4.3 logs — 生活記録

| キー | 内容 | 型 | 値 |
|---|---|---|---|
| `id` / `dt` | ID / 日時 | string | − |
| `kind` | 種類 | string | `wake`(起床) / `sleep`(就寝) / `stool`(排便) / `urine`(排尿) |
| `bristol` | 便の状態 | number\|null | 1〜7。`kind` が `stool` のときのみ保持 |
| `memo` | メモ | string | − |

ブリストル便形状スケール：1 コロコロ便／2 硬い便／3 やや硬い便／4 普通便／5 やや軟らかい便／6 泥状便／7 水様便

### 4.4 steps — 歩数

歩数は1日単位の集計値であり、時刻を持つ記録ではない。そのため配列ではなく **`"YYYY-MM-DD": 歩数` の対応表** で保持する。

この形にすることで、同じ日を何度取り込んでも上書きになり二重計上が起きない。外部（ショートカット）から繰り返し取り込まれる前提のデータなので、冪等であることを優先した。

- 入力範囲：0〜200000
- 同じ日付に保存すると上書きする
- 歩数欄を空にして保存すると、その日の記録を削除する

### 4.5 quick — 食事のワンタップボタン

食事カードに並ぶボタンの定義。設定タブから編集できるため、定数ではなくユーザーデータとして持つ。

| キー | 内容 | 備考 |
|---|---|---|
| `id` | ボタンのID | 初期設定は `q-mugicha` などの固定値 |
| `name` | 名前 | **必須**。記録の「内容」になる |
| `icon` | アイコン | 絵文字など。空なら「・」を表示。設定では絵文字を並べてタップで選ぶ（直接入力も可、先頭4文字まで） |
| `type` | 区分 | `null` なら記録時の時刻から自動で決める |
| `kcal` / `protein` / `salt` | 成分 | それぞれ省略可。範囲は食事の入力と同じ |

- 並び順が画面での表示順になる。追加したものは末尾に付く
- 空の配列も保持する（すべて削除した状態を維持できる）。`defaultQuickItems()` で初期設定に戻せる
- ボタンを削除しても、それで記録した過去の食事は残る
- バックアップJSONに含まれる。`quick` を持たない古いバックアップを読み込んだ場合は初期設定を入れる

### 4.6 settings — 設定

| キー | 内容 | 用途 |
|---|---|---|
| `height` | 身長 (cm) | BMI計算 |
| `goal` | 目標体重 (kg) | 目標差の表示、グラフの目標ライン |
| `kcal` | 1日の目標カロリー | 残りカロリー表示、カロリー棒グラフの基準線 |
| `protein` | 1日の目標たんぱく質 (g) | ヘッダーの進捗表示 |
| `alcLimit` | 1日の純アルコール上限 (g) | 超過判定、アルコール棒グラフの基準線 |
| `saltLimit` | 1日の塩分上限 (g) | 超過判定、塩分棒グラフの基準線 |
| `stepGoal` | 1日の目標歩数 | 達成日数の集計、歩数棒グラフの基準線 |

すべて任意。未設定の項目に依存する表示は「—」または非表示になる。

---

## 5. 計算仕様

| 項目 | 式・規則 |
|---|---|
| **BMI** | `体重kg ÷ (身長m)²`。判定は 18.5未満＝低体重／25未満＝普通／30未満＝肥満1度／35未満＝肥満2度／それ以上＝肥満3度以上 |
| **純アルコール量 (g)** | `酒量ml × 度数% ÷ 100 × 0.8`（比重0.8）。小数第1位に丸める |
| **睡眠時間** | 時系列順に並べ、`sleep` を保持したまま次に現れた `wake` とペアにする。差が **0時間超24時間以内** のペアのみ採用し、**起床した日**の睡眠時間として集計する |
| **7日平均体重** | 直近7日間の体重記録の単純平均。前の7日間との差を「kg/週」として表示 |
| **7日移動平均（グラフ）** | 各点について、その点から遡って7日以内に入る点の平均 |
| **日別集計** | カロリー・純アルコール・睡眠時間は日単位で合算する |
| **休肝日** | 期間内で **記録のある日** のうち、純アルコールが0だった日数。アプリを使わなかった日は分母に含めない |

### 警告表示のしきい値

| 項目 | 条件 |
|---|---|
| 体温 | 37.5℃ 以上 |
| SpO₂ | 96% 未満 |
| 摂取カロリー | 目標値を超過 |
| 純アルコール | 上限値を超過 |

これらは記録を色分けして気づきやすくするためのものであり、**医学的な診断・判定ではない**。

---

## 6. 機能仕様

### 6.1 記録タブ

**画面の構成**
ヘッダー（スクロールしても常に表示）に、共通の日時と **生活／食事／体組成／歩数** の切り替えタブを置く。入力カードは選んだ1枚だけを表示し、上下にスクロールして探す必要をなくす。サマリーは入力カードの下に置き、記録した直後に今日の合計が変わるのを確認できるようにする。

- 最後に選んだタブは端末に保存し、次に開いたときもそのタブから始まる（初回は「生活」）
- 履歴から記録を開いて編集するときは、その記録の種類のタブに自動で切り替える
- 切り替えタブと日時は記録タブでのみ表示する

1.12.0 より前は、サマリーを先頭に置いて4枚の入力カードを縦に並べていた。サマリーが入力欄を押し下げて入力しにくかったため、この構成に変えた。

**サマリー**（記録があるものだけ表示）
最新体重（前回比つき）／体脂肪率／7日平均（週あたり増減）／BMI／目標まで／今日の摂取カロリー／今日のお酒（0gなら「休肝日」）／体温／SpO₂（脈拍をサブ表示）／室温（湿度・天気をサブ表示）／昨日の歩数（目標・今日の歩数をサブ表示）／直近の睡眠（就寝→起床の時刻つき）／今日の排便回数（時刻の一覧つき）

ヘッダーには日付と、目標たんぱく質を設定している場合はその日の進捗（`P 42/110g`）を表示する。

**共通の日時（ヘッダーに固定）**
日時の入力欄は **アプリ全体で1つだけ** とし、ヘッダー内に置いてスクロールしても常に見える状態にする（記録タブでのみ表示）。体組成・生活記録・食事は、ワンタップのボタンを含めてすべてこの日時で記録される。歩数だけは時刻を持たない日単位の値なので、独立した日付欄を持つ。

状態は2つある。

| 状態 | 挙動 | 見た目 |
|---|---|---|
| 追従（既定） | 30秒ごと・タブ復帰時・記録タブを開いたときに現在時刻へ更新する | 通常。ボタンは「現在時刻」 |
| 固定 | 手で日時を変えた時点で追従を止め、その日時を使い続ける | 枠が強調色。ボタンは「今に戻す」 |

固定は**新規作成では維持する**（同じ時刻で続けて何件も入れられるように）。一方、履歴から記録を開いて編集したときは、保存しても取りやめても**現在時刻に戻す**。編集した記録の古い日時が残ったままだと、次のワンタップ記録が意図せず過去の日時で入ってしまうため。

1.9.0 より前は3つのフォームがそれぞれ日時欄を持っていた。入力欄が複数あって分かりにくいため、1か所に統合した。

**生活記録**
4つのボタン（🌅起床／🌙就寝／💩排便／💧排尿）をタップすると、**上部の共通の日時で即座に1件記録**する。
折りたたみ内で、排便時のブリストルスケール選択とメモの入力ができる。ブリストルの選択欄は種類が「排便」のときだけ表示され、同じ番号をもう一度押すと選択解除になる。

**食事**
カード上部にワンタップ記録のボタンを置く。タップすると上部の共通の日時で即座に1件記録する。実際と違うときは履歴からその記録を開いて修正する。

ボタンの内容は **設定タブから追加・変更・削除できる**（§4.5）。初期設定は次の9つ。

| ボタン | 区分 | kcal | たんぱく質 | 塩分 |
|---|---|---|---|---|
| 麦茶 / 緑茶 / コーヒー / 水 / 炭酸水 | 飲み物 | 0 | — | 0 |
| ナッツ | 時刻から自動 | 158 | 4.5g | 0 |
| バナナ | 時刻から自動 | 86 | 1.1g | 0 |
| 味噌汁 | 時刻から自動 | 40 | 3.0g | 1.2g |
| 玄米0.5合 | 時刻から自動 | 260 | 5.0g | 0 |

食べ物の kcal・たんぱく質は、ユーザーが成分表をもとに単品で記録した値にそろえてある。
食べ物の区分は、朝食にも昼食にも食べるものが多いため固定せず、共通の日時の時刻から自動で判定する（区分の判定規則は下記）。
区分は共通の日時の時刻から自動で初期選択する（10時前＝朝食／15時前＝昼食／21時前＝夕食／それ以降＝間食）。
**「食事を記録」ボタンは、カロリー・たんぱく質・塩分の行の直下**に置く。お酒とメモはその下に続く。お酒やメモを使わない記録が大半で、ボタンが画面の下にあると押しに行くのが遠いため。
**お酒のプリセット**は、量(ml)あたりの成分と既定の度数を持つ。

| キー | 名前 | kcal/ml | たんぱく質 g/ml | 既定の度数 |
|---|---|---|---|---|
| `beer` | ビール | 0.40 | 0.003 | 5% |
| `wine_red` | 赤ワイン | 0.68 | 0.002 | 12% |
| `wine_wht` | 白ワイン | 0.75 | 0.001 | 12% |

ボタンは ビール350ml／ビール500ml／赤ワイン120ml／白ワイン120ml。押すと `calcDrink(キー, 量, 度数)` で内容・酒量・度数・カロリー・たんぱく質・塩分(0)を一度に入れる。カロリーは整数、たんぱく質と純アルコールは小数第1位に丸める。

プリセットから入れた後に**量や度数を書き換えると、名前・カロリー・たんぱく質を計算し直す**（ビール700ml → 280kcal・2.1g）。カロリー／たんぱく質は量だけで決まり、度数は純アルコール量にだけ効く。**内容を手で書き換えた時点で自動計算はやめる**（プリセットではない飲み物として扱う）。履歴から「ビール350ml」のようなプリセット由来の名前の記録を開いた場合は、プリセットを割り出して同じく量の変更に追従させる。

入力に応じて純アルコール量をその場で表示する。
直近120件から使用頻度の高い内容を上位8件まで「よく使う内容」として **「内容」欄の直下** に表示し、タップでカロリー・たんぱく質・酒量・度数まで補完する。補完は**毎回入れ替える**。その項目に値が無ければ欄を空にするので、続けて別の項目を押しても前の値が残らない。同じカード内にボタンがあるもの——ワンタップの項目と、お酒のプリセットから作られる名前（`ビール〇〇ml`・`赤ワイン〇〇ml`・`白ワイン〇〇ml`、旧版の `ワイン〇〇ml`）——は、記録数が多くても導線が重複するのでこの一覧から除外する。量が違っても除外する。これら以外の、手入力した飲み物や他の酒は従来どおり並ぶ。除外した結果が0件になる場合は見出しごと隠す。

**体組成・バイタル・環境**
§4.1 の各項目を入力して保存する。日時は上部の共通欄で決まる。

**歩数**
日付（既定＝当日）と歩数を入力して保存する。日付の左右に **前日** / **翌日** ボタンを置き、1日ずつ動かして任意の日を指定できる。未来の日付には進めない。日付を変えると、その日に記録済みの値を読み込む。**貼り付け** ボタンでクリップボードから数値を取り込む（§9）。

### 6.2 グラフタブ

期間は **1週間／1ヶ月／3ヶ月／半年／全期間** から選ぶ。

### 表示期間の移動

期間の下に移動用の行を置き、選んだ期間の幅を保ったまま過去へさかのぼれる。

| 操作 | 動き |
|---|---|
| ◀ / ▶ | 期間の幅だけ前後にずらす |
| 開始日（日付入力） | 表示する期間の開始日を直接指定する |
| 最新 | 今日で終わる期間に戻す |

内部では「開始日」を持ち、`null` のときは今日で終わる窓（＝最新）を表す。◀▶ や日付指定の結果が未来にはみ出す場合は `null` に戻すため、今日より先は表示されない。最新表示中は ▶ と「最新」を無効にする。全期間では移動できないので行ごと隠す。

**この期間はカード内のすべてのグラフに適用される。** 折れ線も棒グラフも同じ窓を見るため、過去にさかのぼるとカロリーや歩数の棒グラフもその期間のものに入れ替わる。棒グラフの本数は最大30日ぶんで、期間が長いときは末尾の30日を表示する。

**折れ線（3モード切替）** — 左軸・右軸の2軸で描画する

| モード | 左軸 | 右軸 | 補助 |
|---|---|---|---|
| 体重・体脂肪率 | 体重 (kg) | 体脂肪率 (%) | 7日移動平均・目標体重ライン |
| 体温・SpO₂ | 体温 (℃) | SpO₂ (%) | − |
| 室温・湿度 | 室温 (℃) | 湿度 (%) | − |

グラフ下の統計欄はモードごとに切り替わる。体重＝期間の増減／最小／最大／平均／体脂肪率の増減、体温＝平均／最高／SpO₂の最低・平均／脈拍の平均、環境＝室温と湿度の平均・最低・最高／最多の天気と内訳。

**棒グラフ（6種）** — 1日の摂取カロリー／たんぱく質／塩分／歩数／睡眠時間／純アルコール量

| グラフ | 基準線 | 超過時 | グラフ下の日数 |
|---|---|---|---|
| 摂取カロリー | 目標カロリー | 赤（上限としての目標） | — |
| 塩分 | 上限 | 赤 | 超えた日 |
| 純アルコール | 上限 | 赤 | 休肝日 |
| たんぱく質 | 目標たんぱく質 | 通常色（下限としての目標） | 達成日 |
| 歩数 | 目標歩数 | 通常色（同上） | 達成日 |
| 睡眠時間 | なし | — | — |

たんぱく質と歩数は多いほど良い値なので、目標線は引くが超過を警告色にはせず、代わりに達成日数をグラフ下に出す。

### 6.3 履歴タブ

すべての記録を日付ごとにまとめ、新しい順に並べる。日付見出しにはその日の体重・摂取カロリー・純アルコール・睡眠時間を表示する。
**すべて／体組成／食事／生活** で絞り込める。🗑 で削除する（削除前に確認する）。

**編集モード**

記録をタップすると記録タブの該当フォームに読み込まれ、編集モードに入る。

- 対象のカードを画面上部までスクロールする（サマリーが長いため、タブを切り替えただけでは入力欄が画面外に残ってしまう）
- カード上部に「編集中：8月26日（水）07:10」の帯と **やめる** を表示する
- 保存ボタンが「この記録を更新」に変わり、その隣に **キャンセル** を表示する
- どちらのキャンセルでも編集を破棄し、新規入力の状態（日時＝現在時刻）に戻る
- 更新しても記録は増えず、同じレコードを書き換える（日時も変更できる）

### 6.4 設定タブ

プロフィール・目標の設定、データの書き出しと読み込み、バージョン確認と更新、全データ削除を行う。全削除は2段階の確認を求める。

---

## 7. 書き出しと読み込み

| 形式 | 用途 | アプリへの読み戻し |
|---|---|---|
| **JSON** | バックアップ・機種変更 | **可能**（追加／置き換え） |
| **CSV** | Excel・Numbers での集計 | 不可（書き出し専用） |

それぞれ「保存」（ファイルとしてダウンロード）と「共有」（`navigator.share` でOSの共有シートへ）を選べる。共有ボタンはファイル共有に対応した端末でのみ表示し、非対応環境では表示しない。共有が失敗した場合はダウンロードにフォールバックし、ユーザーが共有シートを閉じた場合は何もしない。

ファイル名は `diet-backup-YYYYMMDD-HHMM.json` / `diet-YYYYMMDD-HHMM.csv`。

### バックアップ日時の扱い

JSONの先頭に、そのファイル自身の素性を持たせる。

```jsonc
{ "exportedAt": "2026-08-26T07:04:32.164Z", "appVersion": "1.5.4", "body": [...], ... }
```

あわせて、**最後にJSONを書き出した日時**を端末側に記録し（`diet-tracker-v1:backup`）、設定タブに表示する。

- 「最後のバックアップ：2026/8/26 07:04（今日）」
- 一度も書き出していなければ「まだ書き出していません」と、書き出しを促す一文
- 14日以上経っていれば「そろそろ書き出しておくと安心です。」を添える

この日時は**端末ごとの情報**なのでバックアップ本体には含めない。含めてしまうと、復元したときに他端末の書き出し時刻が復活してしまうため。
CSVは復元できないためバックアップとして数えず、書き出しても日時は更新しない。

### JSONの読み込み

- **追加読み込み（マージ）**：既存データを残し、`id` が重複しないレコードだけを追加する
- **置き換え読み込み**：ファイルを読んでから、その内容を示して確認する

置き換えの確認では、読み込もうとしているバックアップの **書き出し日時・アプリのバージョン・件数** と、いま消える **現在のデータの件数** を並べて表示する。読み込み後は結果を画面に残し、どのバックアップを適用したか後から確認できるようにする。`exportedAt` を持たない古いバックアップは「不明（書き出し日時を持たない古い形式）」と表示する。

`body` と `meals` が配列であることを検証し、条件を満たさないファイルは読み込まない。`logs` が無い旧バックアップも読み込める。

### CSVの形式

先頭にBOMを付与（Excelでの文字化け防止）、改行はCRLF、カンマ・引用符・改行を含む値は `"` で囲んでエスケープする。22列固定で、体組成・食事・生活記録・歩数が新しい順に並ぶ。「種別」列が **体組成 / 食事 / 生活 / 歩数** の4値なので、表計算ソフト側でフィルタして目的の記録だけ取り出せる。

```
種別,日時,体重kg,体脂肪率%,体温℃,SpO2%,脈拍bpm,天気,室温℃,湿度%,区分,内容,kcal,たんぱく質g,塩分g,酒量ml,度数%,純アルコールg,便の状態,睡眠時間h,歩数,メモ
体組成,2026/9/12 7:10:00,68.6,24.3,36.5,98,62,晴れ,24.0,55,,,,,,,,,,,,起床後
食事,2026/9/12 19:30:00,,,,,,,,,夕食,鮭の塩焼き・味噌汁・野菜,665,38,3.2,,,,,,,
食事,2026/9/12 20:00:00,,,,,,,,,飲み物,ビール500ml,200,,,500,5,20,,,,
生活,2026/9/12 6:20:00,,,,,,,,,起床,,,,,,,,,7.17,,よく眠れた
生活,2026/9/12 7:40:00,,,,,,,,,排便,,,,,,,,4 普通便,,,
歩数,2026/9/12,,,,,,,,,,,,,,,,,,,8432,
```

「睡眠時間h」は起床の行にのみ入る（就寝とペアになった場合）。歩数の行は日付のみで時刻を持たない。

---

## 8. オフラインと更新

### Service Worker

`https://` で配信されている場合のみ登録する（`file://` では登録しない）。
インストール時に `index.html`・マニフェスト・アイコン一式をキャッシュし、以後は **キャッシュ優先＋裏側で再取得**（stale-while-revalidate）で応答する。キャッシュ名を変えることで古いキャッシュを破棄する。

### バージョン表示

設定タブに、動作中のバージョン・ビルド日・Service Workerの稼働状態を表示する。

### 更新フロー

「更新を確認」は3つの状態を持ち、**どの段階でもユーザーが中断できる**。自動での再読み込みは行わない。

```
[ 更新を確認 ]                 通常
[ 確認中… ] [ キャンセル ]      問い合わせ中（2.5秒でタイムアウト）
[ 今すぐ更新 ] [ あとで ]       新しい版を検知
```

| 操作 | 結果 |
|---|---|
| 確認中にキャンセル | 中断して通常状態に戻る。以後、遅れて届く応答は状態を書き換えない |
| 今すぐ更新 | 再読み込みして新しい版を適用する |
| あとで | 再読み込みしない。取得済みなので、再度「更新を確認」を押すか開き直せば適用される |
| 新しい版が無い | 「すでに最新です」と表示 |

### リリース手順

`index.html` の `APP_VERSION` / `APP_BUILD` と、`sw.js` の `CACHE` を **同じバージョンに揃えて** 配信する。`CACHE` 名が変わることでブラウザが新しい Service Worker と認識し、端末に更新が届く。

---

## 9. ヘルスケア（HealthKit）との連携

**Webアプリから HealthKit を直接読むことはできない。** HealthKitにWeb APIが存在せず、Safari／PWAからは参照する手段がない。ネイティブアプリ化しない限り解決しない、iOS側の制約である。

このため歩数は「ショートカット」アプリを経由して取り込む設計とし、アプリ側に2つの受け口を用意する。

ショートカット側では、歩数の集計範囲を「開始日 **が今日である**」で指定する。他の選択肢（「が次の過去の期間内」＝実行時刻から遡る窓、「が次の範囲内」＝固定日のカレンダー選択）ではカレンダー上の1日を毎日自動で切ることができない。当日ぶんが取れるので、夜に実行して翌朝取り込めば前日ぶんとして扱える。

### 受け口1：クリップボード

歩数カードの **貼り付け** ボタンが `navigator.clipboard.readText()` を呼び、文字列から歩数（と、あれば日付）を取り出す。`8,432 歩` のような桁区切り・単位、全角数字、`2026-08-25,8432` のような日付つきにも対応する。日付が含まれていれば日付欄も合わせて切り替える。

クリップボードAPIは、ブラウザの対応・ペースト許可・クリップボードの内容のいずれでも失敗しうる。そのため歩数欄は数値専用ではなく**テキスト入力**とし、長押しからの手動ペーストを常に受け付ける。保存時に全角を半角へ直し、数字以外を除去して解釈する。ボタンが失敗したときは、原因（未対応／空／数字なし／値が大きすぎる／許可されなかった）を示したうえで手動ペーストへ誘導する。

### 受け口2：URLパラメータ

`?steps=8432&date=2026-08-25` で開かれた場合、起動時に取り込む。

- `steps`：数字以外を除去して解釈する（`9,876` も可）。0〜200000 の範囲外は無視する
- `date`：`YYYY-MM-DD` のほか `yesterday` / `today` を受け付ける。省略時は前日
- 取り込み後は `history.replaceState` でクエリを取り除き、再読み込みで重複処理が起きないようにする
- 日付キーへの代入なので、同じ日を何度取り込んでも結果は変わらない（冪等）

### 既知のリスク

**バックグラウンド実行とクリップボードは併用できない。** iOSは自動実行（オートメーション）で動くショートカットのクリップボード操作を制限しており、深夜に自動実行しても翌朝クリップボードが空になっていることがある。実機で確認済み（手動実行では成功、自動実行では失敗）。このため自動実行に依存する運用は案内しない。歩数は**寝る前に手動実行してその場で取り込む**か、日付を前日／翌日ボタンで動かして後から入力する。

iOSではホーム画面に追加したWebアプリとSafariで保存領域が分かれる場合がある。ショートカットからURLを開くとSafariが起動し、ホーム画面のアプリとは別の場所に記録される可能性がある。**この挙動は実機で未検証**。

---

## 10. 制約と非対応事項

**データの置き場所**

- データは記録した端末のブラウザ内にのみ存在する。端末間の同期・クラウドバックアップは行わない
- ブラウザのサイトデータ削除、プライベートブラウズ、ストレージ逼迫時にデータが失われる可能性がある。復旧手段はユーザーが取ったJSONバックアップのみ
- 保存に失敗した場合はその旨を通知する

**機能上の制約**

- CSVは書き出し専用。読み戻しはJSONのみ
- 天気は数値でないためグラフには描かず、集計は「最多の天気と内訳」で表す
- 食品データベース、カロリーの自動推定、外部サービス連携（体組成計・気象API等）は持たない。歩数のみショートカット経由での取り込みに対応する（§9）
- 通知・リマインダー機能は持たない
- 単位はメートル法固定（kg／cm／℃／ml）。ポンドや華氏には対応しない
- 日本語のみ。多言語化は行わない
- 複数ユーザーの記録を1つの端末で分けて管理することはできない

**性質上の注意**

本アプリは自己記録のための道具であり、医療機器ではない。体温・SpO₂などの色分けはしきい値による単純な強調表示にすぎず、診断・治療の判断に用いてはならない。体調に不安がある場合は医療機関を受診すること。

---

## 11. 変更履歴

| バージョン | 内容 |
|---|---|
| （初版） | 体重・体脂肪率・食事の記録、グラフ、JSON/CSV書き出し、オフライン対応 |
| （〃） | 書き出しをiOSの共有シートに対応 |
| 1.1.0 | 設定にバージョン表示と更新確認を追加（ここからバージョン管理を開始） |
| 1.2.0 | アルコール記録（純アルコール自動計算）、履歴のタップ編集 |
| 1.3.0 | 起床・就寝・排泄の記録、睡眠時間の自動計算 |
| 1.4.0 | 体温・SpO₂・脈拍の記録、折れ線グラフのモード切替を導入 |
| 1.5.0 | 天気・室温・湿度の記録、グラフに「室温・湿度」モードを追加 |
| 1.5.1 | アプリ更新の確認をキャンセル可能に。自動再読み込みを廃止 |
| 1.5.2 | 記録の編集にキャンセルボタンを追加し、編集時に対象カードへスクロールするよう修正 |
| 1.5.3 | すべての日時欄に、現在時刻へ差し替える「更新」ボタンを追加 |
| 1.5.4 | バックアップに書き出し日時を記録。設定に最終バックアップ日時を表示し、読み込み時に対象の素性を確認できるようにした |
| 1.6.0 | 歩数の記録を追加。ショートカット経由（クリップボード／URLパラメータ）での取り込みに対応 |
| 1.6.1 | 歩数の日付に「今日」ボタンを追加 |
| 1.6.2 | 歩数欄をテキスト入力にして手動ペーストを受け付けるよう変更。桁区切り・単位・全角数字・日付つき文字列に対応 |
| 1.7.0 | 歩数の「まとめて取り込み」を追加。クリップボードAPIに依存せず、何日ぶんでも貼り付けて取り込めるようにした |
| 1.8.0 | 生活記録を最上段へ。アルコールのプリセットをビール・ワインに整理。歩数を前日／翌日ボタンで日付指定する形に変更（まとめて取り込みは廃止）。食事に麦茶・炭酸水のワンタップ記録を追加 |
| 1.8.1 | ワンタップ記録に緑茶・コーヒー・水を追加 |
| 1.8.2 | 記録タブの食事と歩数の並びを入れ替え |
| 1.8.3 | 日時欄のボタンのラベルを「更新」から「現在時刻」に変更 |
| 1.8.4 | 体組成カードを食事の下（歩数の上）へ移動。よく使う内容を「内容」欄の直下に移動 |
| 1.8.5 | ワンタップボタンにある飲み物を「よく使う内容」から除外 |
| 1.8.6 | アルコールのプリセットも「よく使う内容」から除外 |
| 1.9.0 | 3つに分かれていた日時入力をヘッダー固定の1か所に統合。既定では現在時刻に追従し、手で変えたときだけ固定する |
| 1.9.1 | ワンタップ記録にナッツを追加（区分・kcal・たんぱく質を項目ごとに持てるよう拡張）。よく使う内容を続けて押したとき、前の項目のカロリー・たんぱく質が残る不具合を修正 |
| 1.10.0 | グラフの表示期間を過去へ移動できるようにし、開始日の指定に対応。たんぱく質の棒グラフを追加 |
| 1.10.1 | ワンタップ記録にバナナ・味噌汁・玄米0.5合を追加。食べ物のボタンは区分を時刻から自動判定するよう変更（ナッツも間食固定をやめた） |
| 1.11.0 | 食事に塩分（食塩相当量）を追加。設定に1日の塩分上限、サマリー・履歴・棒グラフ・CSVに反映 |
| 1.12.0 | 記録タブの入力カードをタブ切り替え（生活／食事／体組成／歩数）に変更し、サマリーを入力カードの下へ移動 |
| 1.13.0 | お酒のプリセットを量あたりの成分つき（ビール・赤ワイン・白ワイン）に変更。押すとカロリー・たんぱく質・塩分まで入り、量を変えると計算し直す |
| 1.14.0 | 食事のワンタップボタンを設定から追加・変更・削除できるようにした。「食事を記録」ボタンをカロリーの行の直下へ移動 |
| 1.14.1 | ボタンのアイコンを絵文字のタップ選択に変更。iPhoneで狭い入力欄のカーソルを動かせず打ち直せなかったため |
